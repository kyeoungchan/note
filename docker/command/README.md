# 🧑🏻‍💻 Docker Command
```shell
# docker-compose.yml 백그라운드로 실행. 로그를 확인하면서 실행하고 싶다면 -d 옵션 생략
docker compose up -d
```

```shell
# 컨테이너 실행
docker exec -it {container_name} bash
```

```shell
# docker-compose 종료
docker compose down
```